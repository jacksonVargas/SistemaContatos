@model ContatoModel

@{
	ViewData["Title"] = "Apagar Contato";
}

<div class="text-center">
    <h2 class="mb-4">Você realmente deseja apagar o contato (@Model.Nome)?</h2>
    <a asp-controller="Contato" asp-action="ApagarContato" class="btn btn-danger" role="button">Apagar</a>
    <a asp-controller="Contato" asp-action="Index" class="btn btn-secondary" role="button">Cancelar</a>
</div>
